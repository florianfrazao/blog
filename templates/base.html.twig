<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('style.css') }}">
    </head>
    <body>
        <header>
            <div id="logo">
                <img src="{{ asset('img/logo.png') }}" alt="logo">
            </div>
            <nav>
                <ul>
                    <li><a href="{{ path('categoryList') }}">Catégories</a></li>
                    <li><a href="{{ path('articleList') }}">Articles</a></li>

                </ul>
            </nav>
        </header>
        <main>
            {% block main %}{% endblock %}
        </main>
        <footer>
        </footer>
    </body>
</html>
